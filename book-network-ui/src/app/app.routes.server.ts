// import { RenderMode, type ServerRoute } from '@angular/ssr';

// // Disable SSR by handling all routes on the client side
// export const serverRoutes: ServerRoute[] = [
//   { path: '**', renderMode: RenderMode.Client }
// ];
